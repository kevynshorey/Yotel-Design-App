[project]
name = "yotel-masterplan"
version = "1.0.0"
description = "YOTEL + YOTELPAD Barbados Masterplan Explorer"
requires-python = ">=3.8"

[tool.setuptools]
packages = ["backend", "backend.engine", "backend.osm", "config"]
